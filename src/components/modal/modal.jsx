import React from 'react'
import propTypes from 'prop-types'

import './modal.scss'

const modal = () => {
  return (
    <div>
      
    </div>
  )
}

Modal.propTypes={
    active:PropTypes.bool,
    id:PropTypes.string
}

export default modal
